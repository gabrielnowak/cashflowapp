<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Motyw bez ActionBar -->
    <style name="Base.Theme.Apka_zaliczeniowa" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green</item>
        <item name="colorControlActivated">@color/buttonColor</item>
        <item name="android:textColor">@color/textMain</item>
    </style>
    <style name="AppPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@android:color/white</item>
    </style>
    <style name="Theme.Apka_zaliczeniowa" parent="Base.Theme.Apka_zaliczeniowa" />
</resources>